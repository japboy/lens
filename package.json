{
  "name": "personal-lens",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "packageManager": "pnpm@11.22.0",
  "engines": {
    "node": "24.19.0",
    "pnpm": "11.22.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "check:language": "node scripts/check-repository-language.mjs",
    "check:publication": "node scripts/check-publication-boundary.mjs",
    "check:rust": "cargo check --manifest-path src-tauri/Cargo.toml --locked",
    "check:dependencies": "cargo deny --manifest-path src-tauri/Cargo.toml check",
    "check": "pnpm run build && pnpm run check:rust",
    "verify": "pnpm run check:publication && pnpm run check:language && pnpm run check && pnpm test && cargo fmt --manifest-path src-tauri/Cargo.toml -- --check && cargo clippy --manifest-path src-tauri/Cargo.toml --locked --all-targets -- -D warnings && cargo test --manifest-path src-tauri/Cargo.toml --locked && pnpm run check:dependencies",
    "tauri": "tauri",
    "test": "vitest run"
  },
  "dependencies": {
    "@fortawesome/fontawesome-free": "7.3.1",
    "@generative-dom/core": "0.4.0",
    "@generative-dom/plugin-cursor": "0.4.0",
    "@generative-dom/plugin-markdown-base": "0.4.0",
    "@generative-dom/plugin-markdown-code": "0.4.0",
    "@generative-dom/plugin-markdown-heading": "0.4.0",
    "@generative-dom/plugin-markdown-inline": "0.4.0",
    "@generative-dom/plugin-markdown-link": "0.4.0",
    "@generative-dom/plugin-markdown-list": "0.4.0",
    "@generative-dom/plugin-markdown-quote": "0.4.0",
    "@generative-dom/plugin-markdown-table": "0.4.0",
    "@tauri-apps/api": "2.11.1",
    "@tauri-apps/plugin-dialog": "2.7.2",
    "@tauri-apps/plugin-opener": "2.5.4",
    "dompurify": "3.4.14",
    "lit": "3.3.3",
    "marked": "18.0.10"
  },
  "devDependencies": {
    "@tauri-apps/cli": "2.11.4",
    "jsdom": "30.0.1",
    "typescript": "7.0.2",
    "vite": "8.2.2",
    "vitest": "4.1.11"
  }
}
